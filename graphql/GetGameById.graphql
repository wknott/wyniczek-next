query GetGameById($id: String!) {
	game(id: $id) {
		id
		name
		thumbnailUrl
		minPlayers
		maxPlayers
		bggRank
		bggWeight
		lastPlayedAt
		inCollection
		pointCategories {
			id
			name
			order
		}
		expansions {
			id
			name
			pointCategories {
				id
				name
				order
			}
		}
	}
}
