query GetPlayerById($id: String!) {
	player(id: $id) {
		id
		name
		records {
			totalPoints
			createdAt
			resultId
			game {
				id
				name
				thumbnailUrl
			}
			expansions {
				id
				name
			}
		}
	}
}
