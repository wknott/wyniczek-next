query GetGamesForScoring {
  games(take: 100, sortBy: ALPHABETICAL) {
    items {
      id
      name
      thumbnailUrl
      pointCategories {
        id
        name
      }
      minPlayers
      maxPlayers
    }
  }
}
