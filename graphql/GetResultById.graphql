query GetResultById($id: String!) {
	result(id: $id) {
		id
		createdAt
		playingTime
		game {
			id
			name
			thumbnailUrl
		}
		expansions {
			id
			name
		}
		scores {
			id
			player {
				id
				name
			}
			points {
				id
				value
				pointCategory {
					id
					name
				}
			}
		}
	}
}
